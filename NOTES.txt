
Ideas for naming rules:

[mappings]
# This is a reverse map so we don't have upteen lines for each kind of java class
JavaArtifact = Controller,ServiceApi,ServiceImpl,*Exception,*Configuration,Repository,PersistenceAdapter
GradleBuildArtifact = GradleBuildScript
GradlePluginArtifact = GradlePluginScript
GradleSettingArtifact = GradleSettings

[naming-rules.className]
_default = Resource.capitalizedName
Controller = { "%sController", Resource.capitalizedName }
ServiceApi = { "%sService", _default }  
ServiceImpl = { "%sServiceHandler", _default } 
ResourceDto = { "%sView", _default }
ResourceEntity = { "%sProjection", _default }

